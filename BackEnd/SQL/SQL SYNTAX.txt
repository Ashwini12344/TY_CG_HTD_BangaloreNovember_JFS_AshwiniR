PROJECTION:-
SELECT */[DISTINCT]COLUMN_NM/EXPRESSION [ALIAS]
FROM TABLE_NM



SELECTION:-										
SELECT */[DISTINCT] COLUMN_NM/EXPRESSION [ALIAS]	      ORDER OF EXECUTION							FROM TABLE_NM                                                   1.FROM
WHERE <FILTER CONDITION>					2.WHERE
								3.SELECT

GROUP  BY:-
SELECT */DISTINCT COLUMN_NM/ EXPRESSION [ALIAS]			1.FROM
FROM TABLE_NM                                                   2.WHERE
[WHERE <FILTER CONDITION>]				        3.GROUP BY                                                            
GROUP BY COLUMN_NM					        4.SELECT

HAVING CLAUSE:-
SELECT */DISTINCT COLUMN_NM/ EXPRESSION [ALIAS]			1.FROM
FROM TABLE_NM							2.WHERE
[WHERE <FILTER CONDITION>]					3.GROUP BY
GROUP BY COLUMN_NM						4.HAVING
HAVING <GROUP FILTER CONDITION>					5.SELECT

ORDER BY :-
SELECT */DISTINCT COLUMN_NM/ EXPRESSION [ALIAS]			1.FROM
FROM TABLE_NM							2.WHERE(ROW BY ROW)
[WHERE <FILTER CONDITION>]					3.GROUP BY(ROW BY ROW)
GROUP BY COLUMN_NM						4.HAVING(GROUP BY GROUP)
HAVING <GROUP FILTER CONDITION>					5.SELECT(GROUP BY GROUP)
ORDER BY COLUMN_NM					        6.ORDER BY(ROW BY ROW)

